@model GameOutputModel

@{
    ViewData["Title"] = "Game Details";
}

<h1>Game Details</h1>

<div>
    <h2>@Model.Title</h2>
    <p><strong>Price:</strong> @Model.Price.ToString("C")</p>
    <p><strong>Genre:</strong> @Model.GameCategories</p>
    <p><strong>Category:</strong> @Model.GameCategories</p>
    <p><strong>Description:</strong> @Model.Description</p>
</div>

<a asp-action="Edit" asp-route-id="@Model.GameId" class="btn btn-warning">Edit</a>
<a asp-action="Index" class="btn btn-secondary">Back to List</a>